# BOA: Better Office Accessibility

BOAは、Microsoft Office用の強力なアクセシビリティ向上ツールスイートであり、NVDAユーザーのスクリーンリーダー体験を大幅に改善するように設計されています。アクセスできないUIコンポーネントを直接修正し、ExcelおよびPowerPoint用の高速ナビゲーションツールを導入します。

---

## ⌨️ ホットキー参照

| 機能 | キーの組み合わせ | コンテキスト / 備考 |
| :--- | :--- | :--- |
| **コマンドモードに入る** | `[Prefix]` (デフォルト: `NVDA+E`) | コマンドプレフィックスモードを有効にします（高音のビープ音が鳴ります） |
| **コマンドモードをキャンセル** | `Escape` | コマンドプレフィックスモードを終了します |
| **EXCELの機能向上** | | |
| **シートレイアウトの分析** | `[Prefix]`, then `L` | データブロックを移動する前にExcel内で実行します |
| **最も近いデータブロックにジャンプ** | `[Prefix]`, then `J` | 事前にレイアウト分析が必要です |
| **一括シートオーガナイザーを開く** | `[Prefix]`, then `X` | アクセシブルなシート並び替えダイアログを開きます |
| **未加工数式のアナウンサー** | `[Prefix]`, then `F2` | 1回押すと、未加工の数式文字列を読み上げます |
| **パワー数式エディター** | `[Prefix]`, then `F2` twice | 2回連続で押すと、アクセシブルな複数行数式エディターを開きます |
| **参照元のトレース** | `[Prefix]`, then `Shift+P` | アクセシブルな方法で参照元のトレースを実行します。 |
| **参照先のトレース** | `[Prefix]`, then `Shift+D` | アクセシブルな方法で参照先のトレースを実行します。セル上でEnterキーを押すと、そのセルに移動します。 |
| **詳細な条件付き書式** | `[Prefix]`, then `F` | フォーカスされているセルの完全な書式の詳細を読み上げます |
| **アクティブシートを左に移動** | `NVDA+Shift+LeftArrow` | アクティブシートを1つ上に移動します |
| **アクティブシートを右に移動** | `NVDA+Shift+RightArrow` | アクティブなワークシートを1つ下に移動します |
| **シートを最初/最後に移動** | `NVDA+Shift+Home` / `End` | ワークシートを絶対的な境界に送ります |
| **行の非表示 / 再表示** | `Ctrl+9` / `Ctrl+Shift+9` | ネイティブのショートカットです。BOAは明示的に表示状態の変更を読み上げます |
| **列の非表示 / 再表示** | `Ctrl+0` / `Ctrl+Shift+0` | ネイティブのショートカットです。BOAは明示的に表示状態の変更を読み上げます |
| **列の再表示 (代替キー)** | `NVDA+Ctrl+Shift+0` | Windowsの入力言語のホットキー衝突を回避します |
| **セルをメモリスロットに割り当て** | `[Prefix]`, then `Shift+1` から `Shift+9` | 現在のセルをバックグラウンド監視スロットに割り当てます |
| **監視中のセルスロットの読み上げ** | `[Prefix]`, then `1` から `9` | 割り当てられたスロットの値を呼び出して読み上げます |
| **スロットへの直接ジャンプ** | `Alt` + `1` から `9` | 監視スロットへカーソルを瞬時に移動します |
| **元のセルにワープバック** | `[Prefix]`, then `\` | スロットを確認した後、直前に作業していたセルに瞬時に戻ります |
| **スロットマネージャーダイアログ** | `[Prefix]`, then `Alt+M` | すべてのアクティブな監視セルを表示・管理するダイアログを開きます |
| **バックグラウンド監視の切り替え** | `[Prefix]`, then `M` | バックグラウンドの再計算の追跡を手動で切り替えます |
| **すべてのメモリスロットをクリア** | `[Prefix]`, then `Backspace` | 保存されているすべてのバックグラウンドセル監視を消去します |
| **POWERPOINTの機能向上** | | |
| **スライドレイアウトの分析** | `[Prefix]`, then `L` | 現在のスライドの空間的レイアウトを分析して読み上げます |
| **ドキュメント分析** | `[Prefix]`, then `D` | 包括的な目次とヘルスレポートを生成します |
| **一括スライドオーガナイザー** | `[Prefix]`, then `X` | 複数のスライドを一括で並び替えるアクセシブルなダイアログを開きます |
| **WORDの機能向上** | | |
| **書式監査** | `[Prefix]`, then `F` | 現在のドキュメントの書式の不整合を監査します |
| **ドキュメント分析** | `[Prefix]`, then `D` | 現在のWordドキュメントのレイアウトと構造を分析します |

---

## 🚀 機能

### Excelの機能向上

#### 1. シートレイアウト分析とキャッシュ
Excelワークシートを即座にスキャンして、その構造、非表示の要素、およびデータブロックを把握します。
* **仕組み:** BOAはシートを迅速にスキャンし、アクティブなデータブロックを読み上げます。また、**非表示のワークシートタブ**、アクティブな**フィルター**、**保護ビュー**、および**非表示の外側の境界**（例：シートの右端付近の列が非表示になっている場合など。これにより、画面外のデータを見落とすことを防ぎます）についても警告します。
* **データナビゲーション:** スキャン後、データブロックジャンプのホットキーを使用して、検出されたデータブロック間でカーソルを即座に移動させることができ、数千もの空のセルを簡単にバイパスできます。

#### 2. 一括シートオーガナイザー
完全にアクセシブルなダイアログを使用して、複数のシートを一度に並べ替えおよび整理します。
* **仕組み:** シートを選択し、新しい位置に割り当てることができるダイアログを開きます。移動が予定されているシートはデータテーブルに一覧表示されます（誤って追加した場合は `Del` キーで削除可能）。`OK` をクリックすると、ワークブックが即座に再配置されます。

#### 3. クイックシートムーバー
キーボードショートカットを使用して、アクティブなシートを左、右、最初、または最後に即座に移動します。

#### 4. アクセシブルなシート名の変更
* シート名を変更する際、NVDAの標準の機能では入力している文字の読み上げが困難な場合があります。
* BOAは `SafeRichEdit` エンジンを使用するカスタムの `ExcelSheetRenameEdit` クラスを挿入します。これにより、名前の変更中に文字、単語、または行単位で正確に読み上げることができます。これは、既存のデフォルトのシート名変更動作に対する機能強化として機能します。

#### 5. 非表示の行/列トラッカー
* グリッド上の移動を事前に追跡し、非表示またはフィルター処理されたデータを見落とすことを防ぎます。
* **分断されたセルの通過:** グリッドの高度に分断された、または非表示のセクションを飛び越えた場合（例えば、行4〜9が非表示になっているため行3から行10に移動した場合）、BOAは「行4から9が非表示」と明示的にアナウンスします。これにより、構造内でデータがスキップされたことを常に把握できます。

#### 6. 条件付き書式アナウンサー
* Excelの条件付き書式ルールによって動的に変更されたセルの色、フォントスタイル、および背景の陰影を自動的に読み上げます。
* セルの生の基礎値だけでなく、実際の視覚的状態を提供します。最初は、セルにフォーカスしたときに「条件付き書式あり、およびその他の軽微な詳細」と読み上げられます。包括的な情報が必要な場合は、詳細ホットキー構成である `NVDA+E` に続いて `F` を使用します。

#### 7. 選択範囲の読み上げの改善
セルまたは範囲が選択されたか、または選択解除されたかを読み上げます。

#### 8. セルモニター:
* **セルモニター:** コマンドパスを使用して、特定のセルをメモリスロットに割り当てます。割り当てられた数字のスロットを使用して、いつでも戻って値を読み上げることができます。
* **継続的な監視:** スロットに登録されたセルはバックグラウンドで自動的に監視されます。Excelが再計算またはセルの編集を実行すると、BOAは即座に新しい値を読み上げます。手動で切り替えるか、コマンドスロットを介してすべてクリアできます。
* **Excel: セルモニターProアップグレード:**
  - **スロットマネージャーダイアログ (`NVDA+E`, then `Alt+M`):** アクティブに監視しているすべてのセルを一覧表示するダイアログを開きます。`Enter` を押すと即座にそのセルにジャンプします。
  - **ワープバック (`NVDA+E`, then `\`):** スロットを確認した後、直前に作業していたセルに瞬時にテレポートして戻ります。
  - **直接スロットジャンプ (`Prefix + Alt` + `Slot Number`):** プレフィックスを完全にバイパスして、割り当てられたセルスロットに即座にジャンプします。

#### 9. パワーエディター
* **Excel: パワーエディター（アクセシブルな数式エディター）:** 巨大な数式を変更するための革新的な機能です。
  - **`NVDA+E` の後に `F2` を1回押す:** アクティブなセルの未加工の数式文字列を即座に読み上げます（数式がない場合は「数式なし」と読み上げます）。
  - **`NVDA+E` の後に `F2` を2回連続で押す:** 巨大な入れ子（ネスト）構造の数式を安全に変更できる、完全にアクセシブルな複数行エディターを開きます。通常の `Enter` キーで改行を挿入して読みやすくし、`Ctrl+Enter` でExcelに保存します。
  - *安全性のチェック:* 数式がシートを破損する前に構文エラーを安全に検出し、計算後のエラー（`#NAME?` や `#DIV/0!` など）を検出して、数式が破損している場合に即座に警告します。

#### 10. 数式監査および評価の強化
* **Excel: 数式監査および評価:** 参照元および参照先を確実に追跡するためのカスタムショートカット（`NVDA+E` の後に `Shift+P`、および `NVDA+E` の後に `Shift+D`）を追加しました。さらに、Excel標準の「数式の評価」ダイアログが完全にアクセシブルになりました。計算をステップ実行するたびに、NVDAが評価結果を自動的に読み上げます！

### PowerPointの機能向上

#### 1. アクセシブルなカラーピッカー
* PowerPointの「色の設定」ダイアログをアンロックします。
* 「赤」、「緑」、「青」の入力ボックスを正しく識別して明示的に読み上げます（`PowerPointRGBEdit` をオーバーライドすることによって実現）。
* これまで見えなかったHex（16進数）入力フィールドをマッピングし、NVDAが完全なHexカラー値をすっきりと読み上げられるようにします。

#### 2. 標準の色グリッドのサポート
* PowerPointの「標準」の色の六角形グリッドを移動すると、通常は「グラフィック」と読み上げられるか、無音になります。
* BOAは六角形グリッド上の矢印キーによる移動を追跡し、非表示のカラー値をバックグラウンドで取得して、リアルタイムでアナウンスします（例：「カラー #FF0000」）。

#### 3. 一括スライドオーガナイザー:
* **PowerPoint: 一括スライドオーガナイザー (実験的機能) (`NVDA+E`, then `X`):** Excelの機能と同様に、完全にアクセシブルなダイアログを使用して、複数のPowerPointスライドを一度に再配置、移動、整理できるようになりました。

#### 4. スライドレイアウトアナウンサー
* **PowerPoint: スライドレイアウトアナウンサー (実験的機能) (`NVDA+E`, then `L`):** 現在アクティブなスライドを即座にスキャンして、その空間的レイアウトとアクセシビリティの制限を把握し、完全にスムーズで応答性の高いスクリーンリーダー体験を保証します。つまり、Excelのシートレイアウト分析と同様に、現在のスライドに関する詳細情報を取得できます。

#### 5. 完全なドキュメント [PPT] 分析
* **PowerPoint: 完全なドキュメント分析 (実験的機能) (`NVDA+E`, then `D`):** NVDAの音声エンジンをフリーズさせることなく、プレゼンテーション全体をマッピングする、高度なバックグラウンド処理アクセシビリティツールです。詳細なナビゲーションが可能な「仮想目次」を提供し、閲覧順序の不整合（表示順序とZ順序の不一致）を検出し、「テキストの壁」スライドにフラグを立て、SmartArtやデータテーブルなどの複雑なオブジェクトをマッピングします。

#### 6. シェイプ移動 [調整] の機能向上:
* **PowerPoint: 図形移動のオーディオモード (実験的機能):** PowerPointキャンバスに3D立体音響の手がかりを導入します。オブジェクトを移動するときにその方向と境界制限を示す音響フィードバックを提供し、空間認識を大幅に向上させます。

### Wordの機能向上:
#### 1. Paul氏のWord Accessアドオンから着想・派生したドキュメント分析:
* **Word: ドキュメント分析 (`NVDA+E`, then `D`):** Wordドキュメントの構造概要を即座に表示します。*(Paul氏への感謝と敬意の特別なメモ: この機能は、彼の素晴らしい「Word Access」アドオンから直接インスピレーションを得たものです。この分野における彼の先駆的な取り組みに深く感謝いたします！)*

#### 2. 書式監査
* **Word: 書式監査 (`NVDA+E`, then `F`):** Wordドキュメントの書式の不整合をスキャンし、視覚的な標準を満たしているか確認します。

#### 3. 脚注リーダー:
* **Word: 自動脚注アナウンサー:** カスタムのBOA設定に基づき、読書中に行内で脚注が自動的に読み上げられるようになります。*(注意: 文末脚注およびコメントのサポートは将来のリリースで予定されています)。*

### インフラストラクチャおよび技術的メカニズム

#### コマンドプレフィックスモード
他のNVDAプラグインとのキー割り当ての衝突を防ぐため、BOAは**コマンドプレフィックスモード**を使用します：
1. 起動ホットキーを押してコマンドモードに入ります。高音のビープ音が聞こえます。デフォルトは `NVDA+E` です。
2. セカンダリキーを押して特定の機能をトリガーします。
3. 無効なキーを押すと、エラーのビープ音が鳴ります。

#### カスタマイズと設定パネル
* BOAの機能は完全にモジュール化されており、いつでも有効または無効にできます。`NVDAメニュー -> 設定 -> 設定 -> BOA Office Enhancements` に移動して、個々の機能をオンまたはオフに切り替えます。
* **インテリジェントアクセラレータキー:** パネル内のすべての設定には、数学的に一意の `Alt+Key` アクセラレータショートカットが割り当てられています。たとえば、`Alt+E` を押すと即座にExcelグループにジャンプし、`Alt+P` はPowerPoint、`Alt+W` はWordにジャンプします。
* 設定は独立したJSONファイル（`boa_settings.json`）に安全に保存されるため、NVDAのコア構成が破損することはありません。
* 将来的にMicrosoft Officeが公式にアクセシビリティのバグを修正した場合、アドオンの他の機能を損なうことなく、BOAの特定のオーバーライドフックのみを安全に無効にすることができます。
* **入力ジェスチャーのカスタマイズ:** すべてのOfficeアプリのすべての機能がNVDA標準の「入力ジェスチャー」ダイアログに明示的に公開されており、すべてのキーボードショートカットを完全に自由にカスタマイズできます。

#### セキュリティと統合境界
* クリップボードの挿入では、ウィンドウのフォアグラウンドプロセスIDを厳密に検証し、他のアプリケーションへのデータ漏洩を防ぎます。
* 一部のカスタムホットキーは、NVDAの「入力ジェスチャー」ダイアログの「Better Office Accessibility」カテゴリの下に完全に公開されています。

---

## 📋 必要条件

* **NVDA:** バージョン 2026.1.0 以降。
* **アプリケーション:** Microsoft Excel および Microsoft PowerPoint。

---

## 💾 インストール

1. 最新の `.nvda-addon` リリースファイルをダウンロードするか、NVDAの標準の「アドオンストア」内で見つけます。
2. ファイルからインストールする場合は、ファイルを開くか、`NVDAのメニュー -> ツール -> アドオンストア -> 外部ファイルからインストール` を使用します。
3. NVDAを再起動します。

---

## 🛠️ 変更履歴

### Version 2.0.0
#### 新機能
* **PowerPoint: 完全なドキュメント分析 (実験的機能) (`NVDA+E`, then `D`):** NVDAの音声エンジンをフリーズさせることなく、プレゼンテーション全体をマッピングする、高度なバックグラウンド処理アクセシビリティツールです。詳細なナビゲーションが可能な「仮想目次」を提供し、閲覧順序の不整合（表示順序とZ順序の不一致）を検出し、「テキストの壁」スライドにフラグを立て、SmartArtやデータテーブルなどの複雑なオブジェクトをマッピングします。
* **PowerPoint: スライドレイアウト分析 (実験的機能) (`NVDA+E`, then `L`):** 現在アクティブなスライドを即座にスキャンして、その空間的レイアウトとアクセシビリティの制限を把握し、完全にスムーズで応答性の高いスクリーンリーダー体験を保証します。つまり、Excelのシートレイアウト分析と同様に、現在のスライドに関する詳細情報を取得できます。
* **PowerPoint: 一括スライドオーガナイザー (実験的機能) (`NVDA+E`, then `X`):** Excelの機能と同様に、完全にアクセシブルなダイアログを使用して、複数のPowerPointスライドを一度に再配置、移動、整理できるようになりました。
* **PowerPoint: 図形移動のオーディオモード (実験的機能):** PowerPointキャンバスに3D立体音響の手がかりを導入します。オブジェクトを移動するときにその方向と境界制限を示す音響フィードバックを提供し、空間認識を大幅に向上させます。上述の通り、これは実験的機能であり、改善のためのフィードバックをお待ちしております。
* **Word: 書式監査 (`NVDA+E`, then `F`):** Wordドキュメントの書式の不整合をスキャンし、視覚的な標準を満たしているか確認します。
* **Word: ドキュメント分析 (`NVDA+E`, then `D`):** Wordドキュメントの構造概要を即座に表示します。*(Paul氏への感謝と敬意の特別なメモ: この機能は、彼の素晴らしい「Word Access」アドオンから直接インスピレーションを得たものです。この分野における彼の先駆的な取り組みに深く感謝いたします！)*
* **Word: 自動脚注アナウンサー:** カスタムのBOA設定に基づき、読書中に行内で脚注が自動的に読み上げられるようになります。*(注意: 文末脚注およびコメントのサポートは将来のリリースで予定されています)。*
* **Excel: パワーエディター（アクセシブルな数式エディター）:** 巨大な数式を変更するための革新的な機能です。
  - **`NVDA+E` の後に `F2` を1回押す:** アクティブなセルの未加工の数式文字列を即座に読み上げます（数式がない場合は「数式なし」と読み上げます）。
  - **`NVDA+E` の後に `F2` を2回連続で押す:** 巨大な入れ子（ネスト）構造の数式を安全に変更できる、完全にアクセシブルな複数行エディターを開きます。通常の `Enter` キーで改行を挿入して読みやすくし、`Ctrl+Enter` でExcelに保存します。
  - *安全性のチェック:* 数式がシートを破損する前に構文エラーを安全に検出し、計算後のエラー（`#NAME?` や `#DIV/0!` など）を検出して、数式が破損している場合に即座に警告します。
* **Excel: 数式監査および評価:** 参照元および参照先を確実に追跡するためのカスタムショートカット（`NVDA+E` の後に `Shift+P`、および `NVDA+E` の後に `Shift+D`）を追加しました。さらに、Excel標準の「数式の評価」ダイアログが完全にアクセシブルになりました。計算をステップ実行するたびに、NVDAが評価結果を自動的に読み上げます！
* **Excel: セルモニターProアップグレード:**
  - **スロットマネージャーダイアログ (`NVDA+E`, then `Alt+M`):** アクティブに監視しているすべてのセルを一覧表示するダイアログを開きます。`Enter` を押すと即座にそのセルにジャンプします。
  - **ワープバック (`NVDA+E`, then `\`):** スロットを確認した後、直前に作業していたセルに瞬時にテレポートして戻ります。
  - **直接スロットジャンプ (`Alt` + `Slot Number`):** プレフィックスを完全にバイパスして、割り当てられたセルスロットに即座にジャンプします。
* **入力ジェスチャーのカスタマイズ:** すべてのOfficeアプリのすべての機能がNVDA標準の「入力ジェスチャー」ダイアログに明示的に公開されており、すべてのキーボードショートカットを完全に自由にカスタマイズできます。

#### UX/UIの向上
* **統合された閲覧可能レポート:** アドオン全体で統一されたHTMLレポートシステムを採用しました。Excel의条件付き書式アナウンサー、レイアウト分析、ドキュメント分析などの機能は、大量のテキストブロックをただ読み上げるだけではなくなりました。その結果がネイティブの移動可能なHTMLウィンドウで開くようになり、自分のペースでデータを確認できます。
* **Excel: 参照先/参照元の追跡の強化:** Excel標準の数式トレースショートカット（直接の参照元は `Ctrl+[`、直接の参照先は `Ctrl+]`）の音声出力を大幅に強化しました。NVDAは選択されたセルを明示的に読み上げるようになりました。
* **Excel: 結合セルのサポート:** 空白スキップセル追跡機能によって、結合セルが正しく検出され、明示的に読み上げられるようになりました。

#### Bug Fixes
* **Word: リスト項目の重複読み上げ:** 一部のWord表示でNVDAが段落のリスト項目を重複して読み上げるバグを修正するため、一時的なパッチを適用しました。
* **Excel: セルモニターのローカライズバグ:** 最近の翻訳ローカライズアップデートによって発生した潜在的な追跡バグを解決しました。

### What's New in v1.6.1
* **ファイル深部のローカライズ**: 100%のローカライズカバー率を確保するため、Excel機能向上モジュール（シートレイアウト分析やクイックシートムーバーなど）の深部で欠落していた文字列の翻訳を修正しました。
* **翻訳サポートの拡大**: システムに7つの新しい言語を追加しました（トルコ語、ポーランド語、韓国語、ウクライナ語、チェコ語、ウルドゥー語、パンジャブ語）。
  *(注意: これらの翻訳はAIによって生成されたものであるため、一部に軽微な翻訳エラーや不正確な表現が含まれている場合があります。)*

### v1.6.0
* **包括的な翻訳サポート**: アドオンは17のグローバル言語をサポートし、完全にローカライズされました。
  *(注意: これらの翻訳はAIによって生成されたものであるため、一部に軽微な翻訳エラーや不正確な表現が含まれている場合があります。)*
* **厳格なコードガバナンス**: コードベース全体にGPL-2.0著作権ヘッダーを適用しました。""",

### Version 1.5.0
#### 新機能
##### データ末尾レーダー
大きなスプレッドシートを移動しているとき、空のセルがリストの末尾を意味しているのか、それとも単にデータにギャップがあるだけなのかを判断するのは難しい場合があります。**データ末尾レーダー**は、空のスペースを盲目的に矢印キーで移動するのを防ぐためのスマートな周囲確認として機能します。
空のセルに移動するたびに、BOAは移動方向の残りのセルを即座にスキャンします。データが全く残っていない場合は、事前に以下のようにアナウンスします：
* *"これより下にはデータはありません"*
* *"これより上にはデータはありません"*
* *"これより右にはデータはありません"*
* *"これより左にはデータはありません"*
**設定オプション:**
この機能は、`NVDAのメニュー -> 設定 -> 設定 -> BOA Office Enhancements` から設定できます。スプレッドシートには非表示の複雑さ（非表示の数式や折りたたまれた行など）が含まれている場合があるため、レーダーは3つの動作モードを提供します：
1. **オフ**: レーダーを完全に無効にします。
2. **厳密なメモリチェック (CountA) [デフォルト]**: 最も安全で高速なアプローチです。スプレッドシートの生メモリをチェックします。足元に*何か*（非表示の行、テキスト、数値、または非表示の数式を含む）を検出した場合、誤報を防ぐために完全に無音のままになります。シートの残りの部分が数学的に100%空白である場合にのみ「データはありません」と読み上げます。
3. **表示データのみ (Math Engine)**: 複雑なシート向けに設計された高度なエンジンです。非表示の行や非表示の数式（例：`=""`）をインテリジェントに除外します。進行方向に実際の表示可能な数値またはテキストが残っている場合にのみ、無音になります。

### Version 1.4 - 2026-06-12
#### 新機能
* **セルモニター:** コマンドパスを使用して、特定のセルをメモリスロットに割り当てます。割り当てられた数字のスロットを使用して、いつでも戻って値を読み上げることができます。
* **継続的な監視:** スロットに登録されたセルはバックグラウンドで自動的に監視されます。Excelが再計算またはセルの編集を実行すると、BOAは即座に新しい値を読み上げます。手動で切り替えるか、コマンドスロットを介してすべてクリアできます。

#### Bug Fixes

### Version 1.3.0 — 2026-06-05
*ファイナルリリース*

#### 新機能
* **シートレイアウト分析:** 強力なレイアウトスキャンインフラを追加しました。検出されたデータブロックをキャッシュしながら、ワークシート保護、アクティブな列フィルター、非表示のワークシートタブ、および非表示の絶対境界を即座に検出します。
* **ガイド付きデータブロックナビゲーション:** 分析後のナビゲーションにより、データの主要なクラスター間を即座にカーソル移動し、空のセルをシームレスにバイパスできます。
* **条件付き書式アナウンサー:** Excelの条件付き書式ルールによって変更されたセルの動的な色、フォントスタイル、および背景の陰影を自動的に検出して読み上げます。
* **明示的な設定アクセラレータ:** NVDAのアーキテクチャに厳密に準拠するように、BOA設定GUIを全面的に刷新しました。すべての機能チェックボックスがグローバルに一意の `Alt+Letter` ショートカットを持つようになり、キーボードサイクリングを防ぎ、最初の文字によるナビゲーションエラーを排除しました。

#### バグ修正
* **絶対的な端の境界検出:** ネイティブのCOM `UsedRange` 端チェックを、絶対的な1次元の数学的境界チェック（`Row 1048576` および `Column 16384`）に置き換え、非表示の行/列がアクティブデータブロックの遥か外側にある場合でも確実に検出できるようにしました。
* **LazyなCOMプロパティの安全な脱出:** 数百万の連続した非表示構造を評価する際のNVDAスレッドのフリーズを防ぐため、COMプロパティループを堅牢化しました。

### Version 1.2.0 — 2026-06-03
*ファイナルリリース*

#### 新機能
* **アプリ起動時のキャッシュ化:** 主要なアーキテクチャの刷新です。Officeアプリケーションにフォーカスしたときにのみコアモジュールが遅延読み込みされるようになり、起動ラグを解消し、名前変更ダイアログでの「不明」オブジェクトフォーカスの不具合を完全に解決し、複数ファイル構成のコードベース構造を維持します。
* **強化されたセル追跡 (1D COM Math):** 非表示セルのギャップ検出ロジックを書き換え、1次元の断面（`current_col` または `current_row`）のみを評価するようにしました。これにより、COM計算の負荷が1,600万セル以上削減され、非表示範囲を飛び越える際のナビゲーションのフリーズが即座に解消されます。
* **プロセスメモリの消去:** ユーザーがExcelを閉じて再度開いたことを検出するため、Excelウィンドウハンドル（`Hwnd`）の追跡を実装しました。これにより、古いグローバル状態のメモリが能動的に消去され、新しく「Book1」を開いたときに誤って「シートが非表示」とアナウンスされる問題が完全に解決されます。

#### バグ修正
* **選択の二重読み上げ:** 信頼性の低い非同期の `winUser.getKeyState` から移行し、`api.getLastInputGesture()` を実装して、Shift+矢印キーを使用する際の二重アナウンスを完全に抑制しました。
* **境界検出器の無効化:** NVDAネイティブのナビゲーション安定性を保護するため、事前に動作する境界検出器を無効化し、全面的にギャップスキップトラッカーにフォールバックするようにしました。

### Version 1.1.0 — 2026-05-30
*ファイナルリリース*

#### 新機能
* **設定GUI:** `NVDAメニュー -> 設定 -> 設定` 内にネイティブの「BOA Office Enhancements」パネルを追加し、簡単に機能をオンまたはオフに切り替えられるようにしました。
* **SafeRichEditフック:** Office 2024でRichEditコントロールを操作する際、NVDAがサイレントクラッシュするのを防ぎます。
* **カスタマイズ可能なホットキー:** すべてのBOAホットキーが、NVDAの「入力ジェスチャー」ダイアログの「Better Office Accessibility」カテゴリの下に完全に公開されるようになりました。
* **Excel: 非表示の行/列スキップ検出:** 非表示の行または列を通り過ぎて移動するときに事前にアナウンスし、フィルター処理されたデータを見落とさないようにします。設定で切り替え可能です。

#### バグ修正
* **スレッドの安全性:** すべてのブロッキング遅延（`time.sleep`）を削除し、ノンブロッキングのNVDA非同期コールバックに置き換えることで、バックグラウンド操作中にスクリーンリーダーが途切れないようにしました。

### Version 1.0.0 — 2026-05-24
*初期パブリックリリース*

#### 新機能
* **Excel: 一括シートオーガナイザー:** 完全にアクセシブルなダイアログを使用して、複数のシートを一度に即座に並べ替えます。
* **Excel: クイックシートムーバー:** キーボードコマンドを介して、アクティブなシートを左、右、最初、または最後に移動します。
* **Excel: アクセシブルなシート名の変更:** アクセスできないネイティブの名前変更フィールドをインターセプトし、信頼性の高いアクセシブルなダイアログに置き換えます。
* **Excel: スマートな選択追跡:** 複数セルの範囲選択および選択解除を正確にアナウンスします。
* **PowerPoint: アクセシブルなカラーピッカー:** NVDAが「色の設定」ダイアログ内でRGBおよびHex値を正確に読み上げられるようにします。
* **PowerPoint: 標準の色グリッドのサポート:** 矢印キーナビゲーションをインターセプトし、アクセスできない色の六角形グリッドから非表示のHexコードを読み取ります。
